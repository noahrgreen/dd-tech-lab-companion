# Validation Notes

- The YAML layout matches the seed pseudocode structure and action names.
- The synthetic dataset is deterministic and sized to the article's 5,000-counterparty claim.
- This bundle validates artifact organization and field-level shape; it does not claim parity with a live Foundry Workshop export.
