# Validation Notes

- Schema objects and link properties match the seed's article-side taxonomy.
- Synthetic data includes 200 entities across five disclosure regimes and temporal ownership properties suitable for as-of-date traversal.
- Queries are written as portable Cypher-style traversal starters; they are not represented as exported Foundry ontology objects.
